shard_embedding()
shard_attention()
shard_mlp()
replace_layernorm()
replace_rotarty_pos_emb()
trace_attention()
subgraphs = sch.find(scaled_dot_product)
sch.replace(EfficientAttention(), subgraphs)
sch[path.replace("N", str(idx))].checkpoint(order_args_fn=order_args_fn)
generate_pipeline_stages()
bcast_input()